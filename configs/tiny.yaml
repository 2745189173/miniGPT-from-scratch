seed: 1337

experiment:
  run_name: e010_shakespeare_gelu

data:
  input_path: data/raw/input.txt

model:
  block_size: 64
  n_layer: 4
  n_head: 4
  n_embd: 128
  activation: gelu
  dropout: 0.1

train:
  batch_size: 32
  max_iters: 6000
  eval_interval: 100
  eval_iters: 20
  learning_rate: 0.0003
  weight_decay: 0.1
  device: cuda

generate:
  prompt: "the"
  max_new_tokens: 300
  temperature: 0.8
  top_k: 20
